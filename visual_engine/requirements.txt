# Note: torch and ultralytics are installed separately in the Dockerfile
# (with --index-url for the CPU wheel). For local dev, install manually:
#   pip install torch>=2.6.0 torchvision>=0.21.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install ultralytics>=8.3.2
torch>=2.6.0
torchvision>=0.21.0
ultralytics>=8.3.2
transformers>=4.48.0
sentence-transformers
Pillow
fastapi
uvicorn
python-multipart
huggingface_hub
prometheus-fastapi-instrumentator
openai-clip
peft>=0.10.0
